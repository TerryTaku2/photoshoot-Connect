import re
import uuid


def slugify(value: str) -> str:
    value = value.lower().strip()
    value = re.sub(r"[^a-z0-9]+", "-", value)
    return value.strip("-")


def new_id() -> str:
    return uuid.uuid4().hex
